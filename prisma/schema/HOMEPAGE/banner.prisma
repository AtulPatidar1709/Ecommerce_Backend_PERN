model Banner {
  id        String   @id @default(uuid())
  title     String
  imageUrl  String
  linkUrl   String?
  description String?
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
}
